<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.rimi.oa.dao.UserMapper">
 	<select id="selectUser" resultType="com.rimi.oa.entity.User" parameterType="com.rimi.oa.entity.User">
 		select * from user where name = #{name} and password = #{passWord}
 	</select>
 </mapper>